# ECHO 진행 기록

## 2026-09-01 — 프로젝트 시작, 문제정의, 센서모델 PE 1개 검증 완료

**시작**: ZERO에서 겪은 실제 병목을 이번 학기 회로설계 수업 프로젝트로 가속해보자는
아이디어로 착수.

**시장조사**: 같은 F1TENTH particle_filter를 FPGA로 가속한 선행논문 실제로 확인함
— Bernardi et al., *"An FPGA Overlay for Efficient Real-Time Localization in
1/10th Scale Autonomous Vehicles"* (DATE 2022). 놀랍게도 ZERO가 쓰는 것과 **완전히
같은 원본 소프트웨어**(Walsh & Karaman, ICRA 2018 reference implementation)를
베이스로 함. 이 논문은:
- Xilinx Zynq UltraScale+ (Ultra96/ZCU102, 산업용 대형 보드)
- Vivado **HLS**로 C++→RTL 자동합성 (손으로 짠 RTL 아님)
- 레이마칭만 하드웨어 가속(전체 지연의 90%), 센서모델 평가는 소프트코어에서
  그대로 실행(데이터 이동만 줄임, 병렬 하드웨어화 안 함)

→ **차별화 지점 확정**: 손으로 짠 RTL, 소형 교육용 보드, 그리고 논문이 안 건드린
센서모델 평가를 v1 타깃으로.

**실측 (가장 중요한 작업)**: `particle_filter.py`의 `fine_timing` 파라미터를 켜고
ZERO 시뮬레이션(`sim:=true planner:=none`, opponent_perception 없이 PF 단독)을
실제로 돌려서 진짜 숫자를 뽑음.
- 레이마칭이 센서모델 계산의 **89.6%(500파티클) / 90~95%(4000파티클)** — 논문
  수치(90%)와 3중 일치
- **500파티클: possible ≈ 620Hz(요구 40Hz 대비 15배 여유) / 4000파티클(설계 목표):
  possible ≈ 88Hz(2.2배 여유)** — 이 개발PC에서는 PF가 병목이었던 적이 한 번도 없음
- 결론: `max_particles`가 4000→500으로 다운그레이드된 채 방치된(2026-08-07~) 진짜
  원인은 PF 자체가 아니라 그때 같이 돌던 `opponent_perception`(O(n²) 버그)일 가능성이
  높음. Jetson 실측은 여전히 미실측(가장 큰 남은 갭) — 상세는
  [`docs/problem_statement.md`](docs/problem_statement.md).

**진단 중 발견한 버그(재발 방지용 기록)**: `ros_clean.sh`와 `ros2 launch ...`를 같은
`bash -lc "..."` 문자열 안에서 연달아 실행하면, `ros_clean.sh`의
`pkill -f 'ros2 launc[h]'`가 **그 문자열 자체(나중에 나올 "ros2 launch" 텍스트를
이미 포함)를 실행 중인 셸 프로세스 자신**에 매칭돼서 자기 자신을 죽여버림(exit 15).
반드시 별개의 명령으로 분리해서 실행할 것. 또한 `bash -lc`(비대화형)는 `.bashrc`의
인터랙티브 체크 때문에 ROS 환경이 자동 로드 안 됨 — 매번 4줄(foxy/venv/install/
PYTHONPATH) 명시적으로 source 필요.

**타깃 확정**: 레이마칭이 임팩트는 훨씬 크지만(3중 확인), **v1은 센서모델 평가로
확정, 레이마칭은 v2.** 이미 설계가 끝나있고 난이도가 첫 프로젝트에 적당함.

**설계 & 구현**:
- `echo-ref/gen_sensor_model.py`(Windows) — ZERO의 `precompute_sensor_model()`
  수식을 그대로 이식. 표를 log(확률)로 저장해서 RTL이 곱셈기 없이 룩업+덧셈만
  하면 되게 설계(squash_factor도 log영역에선 상수곱). 실측 log(prob) 범위
  [-9.10, -2.12]에서 역산한 **Q5.8(13비트)** 고정소수점, 테이블 301×301(15m/5cm),
  전체 143.8KB(추후 보드 BRAM 예산과 대조 필요).
- `rtl/sensor_pe.v` — PE 1개. BRAM 1클럭 읽기지연에 맞춘 파이프라인(주소 제시 →
  다음클럭 데이터 도착 → 누적). `start`/`valid_i`/`last_i`/`done` 인터페이스.
- `rtl/table_mem.v` — `.hex` 로드하는 동기식 BRAM.
- `tb/tb_sensor_pe.v` — 파이썬이 만든 파티클 5개(8빔 장난감 시나리오)로 검증.

**검증 — 첫 실행 2/5 실패, 원인 규명 후 5/5 통과**: 처음 돌렸을 때 5개 중 2개가
±1 LSB(=1/256) 차이로 실패. 원인은 RTL 버그가 아니라 **정답지 생성 방식의
반올림 순서 실수**였음 — 파이썬이 "float 8개를 다 더한 뒤 한 번에 반올림"했는데,
RTL(진짜 하드웨어)은 "이미 반올림된 표 값 8개를 정수로 더함" — 이 둘은 수학적으로
다른 결과가 나올 수 있음. 실제로 문제였던 두 파티클의 표 항목을 직접 더해보니
RTL 결과(-12229)가 맞고 파이썬 정답지(-12230)가 틀렸음을 확인. `gen_sensor_model.py`
를 "양자화된 표 값을 정수로 더하는" 방식으로 수정 → 재생성 → **5/5 완전 일치.**

**60빔 확장 + PE 2개 병렬화 (같은 세션 이어서 진행)**:
- `gen_sensor_model.py`의 테스트벡터 출력 방식을 바꿈 — 예전엔 사람이 읽는
  텍스트(`obs=[...] addrs=[...]`, 파이썬 리스트 형식)를 Verilog 소스에 손으로
  옮겨적었는데, 빔이 8→60개로 늘면서(파티클 5개 x 60빔 = 300개) 손으로 옮겨적는
  게 비현실적이 됨. `testvec_addrs.hex`/`testvec_expected.hex` 두 개로 바꿔서
  테스트벤치가 `$readmemh`로 직접 읽게 함 — 데이터를 소스에 손으로 옮기지 않는
  게 원칙(오타를 RTL 버그와 헷갈릴 여지를 원천 차단).
- `tb/tb_sensor_pe.v`를 60빔 기준으로 다시 검증 — **5/5 PASS**, RTL은 한 글자도
  안 고침(PE가 빔 개수를 하드코딩 안 해서).
- **병렬화**: `rtl/table_mem_dp.v`(듀얼포트 BRAM, 포트 2개가 같은 `mem` 배열을
  각자 독립적으로 읽음 — 테이블을 2벌 복사하는 게 아니라 진짜 듀얼포트 메모리
  1개로 인식되는 표준 패턴) + `tb/tb_sensor_pe_parallel.v`(PE 2개를 `fork/join`
  으로 정확히 같은 타이밍에 구동, 각각 파티클 0/1을 동시 처리).
- 결과: **두 PE 다 정답과 일치**, 그리고 **총 소요시간이 640ns**(≈ 파티클 1개
  처리 시간, 62클럭×10ns)로 — 순서대로 처리했으면 거의 2배 걸렸을 텐데 그렇지
  않았음 = **진짜 동시 처리가 검증됨.**

**PE 4개로 확장**: BRAM 포트가 2개뿐이라(듀얼포트), PE 4개를 진짜로 동시에
돌리려면 **테이블 복사본 2개**가 필요함(PE0,1은 복사본0 / PE2,3은 복사본1) —
BRAM 143.8KB → 287.6KB(2배), 나중에 보드 정해지면 이 비용이 부담되는지 판단 필요.
`sensor_pe.v`/`table_mem_dp.v`는 손 안 대고 `generate`로 4개/2개 인스턴스화만 함.

**🔴 무한루프 버그, 원인 규명**: 처음 돌렸을 때 `vvp`가 CPU 100%로 6분 넘게
안 끝남(진짜 무한루프, 멈춘 게 아니라 계속 도는 중이었음 — `clk`가 영원히
토글되는데 `$finish`에 도달을 못 함). 원인: `task drive_pe(pe, idx)` 하나를
`fork`로 4번 동시에 호출했는데, Verilog `task`는 기본이 **정적(static)**이라
동시 호출끼리 내부변수(`rr`, 심지어 입력 `pe`/`idx`까지)를 **공유**함 — 4개가
서로 덮어쓰면서 최소 1개 PE가 `last_i`를 제대로 못 받아 `done`이 영원히 안 뜸.
(PE 2개 버전은 `drive_pe0`/`drive_pe1`을 아예 다른 task로 나눠서 우연히 이
문제를 피했었음.) **해결**: `task automatic drive_pe(...)`로 `automatic` 키워드
추가 — 호출마다 독립된 저장공간을 쓰게 함. 재컴파일 후 **4/4 PASS, 총 소요시간
640ns**(PE 2개 버전과 완전히 동일) — 파티클이 2개든 4개든 처리시간이 그대로라는
게 "진짜 병렬"이라는 걸 다시 한번 확인함.

**"진짜 4배냐" 직접 대조 실측**: 지금까지는 "PE 2개일 때도 640ns, 4개일 때도
640ns"라는 간접 증거만 있었음. `tb/tb_sensor_pe_seq4.v`(PE 1개로 파티클 4개를
순서대로 처리하는 대조군)를 새로 만들어서 직접 비교:
- PE 1개, 파티클 4개 순서대로: **2560ns**
- PE 4개, 파티클 4개 동시에: **640ns**
- 2560 ÷ 640 = **정확히 4.0배** — 추측이 아니라 같은 조건 직접 대조로 확정.

**남은 것**:
1. ~~8빔 장난감 → 실제 스펙 60빔~~ — ✅ 완료
2. ~~PE 2개 병렬화~~ — ✅ 완료(듀얼포트 메모리 공유 방식으로 검증)
3. ~~PE 4개로 확장~~ — ✅ 완료(테이블 복사본 2개, task automatic 버그 수정 포함)
4. PE를 더 늘리기(8개, 16개...) — 테이블 복사본을 계속 늘리는 게 맞는지, 아니면
   어느 지점부터 시분할(중재) 로직으로 바꿔야 하는지 — 보드 BRAM 예산 알아야 결정 가능
5. 주소생성 로직 — 지금은 테스트벤치가 `r*301+d`를 미리 계산해서 줌. 301이
   2의 거듭제곱이 아니라 곱셈 없이 하드웨어로 만드는 게 아직 v1 스코프 밖
6. Jetson에서 `fine_timing` 실측 — problem_statement.md의 가장 중요한 미해결 갭
7. FPGA 보드 확정되면 합성(Vivado/Quartus) — 비트폭·PE개수·BRAM예산 최종 확정
8. v2: 레이마칭 PE (더 어려움 — 가변 길이 격자 순회, 훨씬 큰 온칩 메모리 필요)
## 2026-09-02 — 주소생성 로직(곱셈기 없이 r*301+d)

**설계**: 301은 2의 거듭제곱이 아니라 시프트만으로 곱셈을 대신할 수 없음. 하지만
"상수" 곱셈은 그 상수를 2의 거듭제곱의 합으로 쪼개서 시프트+덧셈으로 풀 수 있음:
```
301 = 256+32+8+4+1 = 2^8+2^5+2^3+2^2+2^0
r*301 = (r<<8)+(r<<5)+(r<<3)+(r<<2)+r
```
진짜 곱셈기(부분곱 N개를 다 더해야 함) 대신 **덧셈 4번**으로 끝남 — `rtl/addr_gen.v`.
테이블을 512로 패딩해서 시프트만으로 푸는 대안도 있었지만(그럼 곱셈이 완전히
사라짐), 테이블이 143.8KB→426.7KB(3배)로 커져서 기각 — BRAM을 아끼는 게 더
중요하다고 판단(PE 병렬화 때 BRAM 비용 문제를 이미 겪어봐서).

**검증 2단계**:
1. `tb/tb_addr_gen.v` — addr_gen 단독, 손으로 계산한 (r,d)→주소 6쌍과 대조. 6/6 PASS.
   (r=300,d=300 → 90600으로 테이블 맨 끝 주소까지 확인함)
2. `tb/tb_sensor_pe_addrgen.v` — addr_gen을 sensor_pe 앞단에 실제로 연결해서,
   테스트벤치가 이제 **주소가 아니라 원본 (r,d) 값만** 주고, 하드웨어가 스스로
   주소를 계산해 룩업+누적까지 끝내는 전체 파이프라인 검증. 5/5 PASS, 예전
   버전(주소 미리 계산해서 줬을 때)과 **정확히 같은 weight 값** — addr_gen이
   기존에 쓰던 정답 주소와 완전히 같은 걸 만들어낸다는 교차검증까지 확보.

**의미**: 이제 RTL 파이프라인에 "테스트벤치가 몰래 대신 계산해준 값"이 하나도
없음 — 룩업 주소부터 누적, 병렬 처리까지 전부 하드웨어 설계로 완성됨. v1(센서모델
가속기) 핵심 요소는 다 갖춰졌다고 볼 수 있음.

**gen_sensor_model.py 변경**: 테스트벡터에 `testvec_r.hex`/`testvec_d.hex`(원본
r,d 쌍) 추가 — 기존 `testvec_addrs.hex`(미리 계산된 주소)는 addr_gen 없이 PE만
테스트할 때 계속 씀.

**다음(3번, 레이마칭 v2)으로 넘어가기 전 남은 v1 항목**:
- PE를 4개보다 늘리기 — 보드 BRAM 예산 알아야 결정 가능(계속 보류)
- Jetson 실측 — 실차 접근 기회 필요(계속 보류)
- 합성 — 보드 확정 필요(계속 보류)

## 2026-09-02 (이어서) — v2 레이마칭 착수, 첫 모듈(ray_march.v) 4/4 검증

**v1은 여기서 일단락**: 센서모델 PE(60빔) + 병렬화(2/4개) + 4배 속도향상 실측 +
주소생성까지 다 갖춰짐. v2(레이마칭)로 전환.

**설계(v2.0, 의도적 단순화)**:
- 스텝 크기 고정 1.0 — 방향벡터(dx,dy)를 그대로 더해가며 전진(곱셈 없음). 진짜
  range_libc의 마칭(거리장 기반, 빈 공간에서 성큼성큼)은 다음 단계 — 이번엔
  기본 골격(격자 걷기+충돌판정+상태기계) 검증이 목표.
- dx,dy(단위 방향벡터)는 외부에서 이미 계산돼서 들어옴 — cos/sin을 하드웨어로
  만드는 건(CORDIC 등) 완전히 다른 주제라 범위 밖.
- 20x20 테스트 지도 하드코딩(x=15 열에 세로 벽 하나) — 실제 트랙 크기 지도는 이후 과제.
- 지도 밖으로 나가는 것도 막힘으로 취급(bagviz의 Grid.collided와 같은 관례).
- Q6.8 고정소수점(정수부 6비트, 소수부 8비트) — 20x20 지도엔 여유있게 충분.

**검증**: , 4가지 시나리오를 손으로 계산한 정답과 대조:
1. 정면(+x) 벽까지: 13스텝 — PASS
2. 옆(+y) 지도경계까지: 18스텝 — PASS (경계도 막힘으로 처리되는지 확인)
3. 45도 대각선으로 벽까지: 19스텝 — PASS (임의 각도에서도 되는지 확인, 고정소수점
   오차 때문에 정확한 45도가 아니라 근사값(181/256≈0.707)을 썼는데도 정확히 맞음)
4. MAX_STEPS를 일부러 짧게(5) 잡아서 아무것도 못 찾는 케이스 — PASS (hit_o=0 확인)

**4/4 전부 첫 시도에 통과** — 센서모델 때처럼 반올림 버그 같은 게 없었음(정수
스텝이라 그런 문제가 애초에 생길 여지가 적음).

**다음(v2 계속)**:
- 실제 트랙 크기 지도(수백x수백 칸) — 지금처럼 배열 인덱싱이 아니라 진짜 BRAM
  주소 계산이 필요해짐(addr_gen처럼 곱셈 없이 만들 방법 다시 고민)
- 거리장 기반 마칭(빈 공간에서 큰 보폭) — 지금은 항상 1칸씩만 감
- 센서모델(v1)과 조합 — 레이마칭 결과(기대거리)를 센서모델 PE 입력으로 바로 연결

## 2026-09-02 (이어서) — v2 레이마칭 착수, 첫 모듈(ray_march.v) 4/4 검증

**v1은 여기서 일단락**: 센서모델 PE(60빔) + 병렬화(2/4개) + 4배 속도향상 실측 +
주소생성까지 다 갖춰짐. v2(레이마칭)로 전환.

**설계(v2.0, 의도적 단순화)**:
- 스텝 크기 고정 1.0 — 방향벡터(dx,dy)를 그대로 더해가며 전진(곱셈 없음). 진짜
  range_libc의 "마칭"(거리장 기반, 빈 공간에서 성큼성큼)은 다음 단계 — 이번엔
  기본 골격(격자 걷기+충돌판정+상태기계) 검증이 목표.
- dx,dy(단위 방향벡터)는 외부에서 이미 계산돼서 들어옴 — cos/sin을 하드웨어로
  만드는 건(CORDIC 등) 완전히 다른 주제라 범위 밖.
- 20x20 테스트 지도 하드코딩(x=15 열에 세로 벽 하나) — 실제 트랙 크기 지도는 이후 과제.
- 지도 밖으로 나가는 것도 "막힘"으로 취급(bagviz의 Grid.collided와 같은 관례).
- Q6.8 고정소수점(정수부 6비트, 소수부 8비트) — 20x20 지도엔 여유있게 충분.

**검증**: tb/tb_ray_march.v, 4가지 시나리오를 손으로 계산한 정답과 대조:
1. 정면(+x) 벽까지: 13스텝 — PASS
2. 옆(+y) 지도경계까지: 18스텝 — PASS (경계도 "막힘"으로 처리되는지 확인)
3. 45도 대각선으로 벽까지: 19스텝 — PASS (임의 각도에서도 되는지 확인, 고정소수점
   오차 때문에 정확한 45도가 아니라 근사값(181/256≈0.707)을 썼는데도 정확히 맞음)
4. MAX_STEPS를 일부러 짧게(5) 잡아서 아무것도 못 찾는 케이스 — PASS (hit_o=0 확인)

**4/4 전부 첫 시도에 통과** — 센서모델 때처럼 반올림 버그 같은 게 없었음(정수
스텝이라 그런 문제가 애초에 생길 여지가 적음).

**다음(v2 계속)**:
- 실제 트랙 크기 지도(수백x수백 칸) — 지금처럼 배열 인덱싱이 아니라 진짜 BRAM
  주소 계산이 필요해짐(addr_gen처럼 곱셈 없이 만들 방법 다시 고민)
- 거리장 기반 마칭(빈 공간에서 큰 보폭) — 지금은 항상 1칸씩만 감
- 센서모델(v1)과 조합 — 레이마칭 결과(기대거리)를 센서모델 PE 입력으로 바로 연결

## 2026-09-02 (이어서) — v2.1: 실제 changwon 트랙 지도(400x160칸)로 확장

**실제 대회 트랙 데이터를 씀**: ~/zero/src/track_assets/maps/changwon/map.pgm
(400x160픽셀 @ 0.05m/px = 20m x 8m, 벡터로 만든 지도라 픽셀이 0/255만 있어 깔끔함).
손그림/벡터PPT로 재구성했던 그 트랙(2026-08-04 세션) 그대로.

**핵심 설계 변화 — 진짜 BRAM 방식 주소 계산**: 20x20 테스트 지도 때는 2차원 배열
인덱싱(`map_row[y][x]`)으로 충분했지만, 이번엔 실제 크기(400x160)라 **평면(flat)
메모리 + 계산된 주소**로 바꿈. 폭을 512(2^9)로 패딩해뒀기 때문에(echo-ref/
gen_track_map.py) `addr = y*512+x = {y,9'b0}|x` — 그냥 비트를 이어붙이기만 하면
됨(addr_gen.v의 301 같은 애매한 상수보다도 쉬운 케이스, 시프트-덧셈도 불필요).
→ `rtl/ray_march_bram.v`. 지도는 셀당 1비트로 저장(`$readmemb`, 81920줄) —
ponytail: 32비트씩 워드로 묶으면 더 알차게 쓸 수 있는데 이번 단계는 스코프 밖.

**소프트웨어 정답지**: `echo-ref/gen_track_map.py` — map.pgm을 읽어 occupancy
배열로 바꾸고, **RTL과 정확히 같은 알고리즘(고정스텝 1.0)을 파이썬으로 재현**해서
6가지 시나리오(트랙 중앙에서 상하좌우+대각선+벽 바로 앞)의 정답을 미리 계산.
지도가 실제 트랙 모양이라 손 계산이 불가능해서 이번엔 소프트웨어로 먼저 걸어봄.

**🔴 실측으로 잡은 비트폭 버그**: 첫 실행 결과 6개 중 `center_+x`(먼 거리, 119칸)
하나만 실패 — RTL이 56에서 멈춤(정답 119). 원인 추적: 56스텝째에 x가 정확히
256.0이 되는데, 정수부를 **9비트 부호있는(signed)** 값으로 뽑고 있었음 — 9비트
signed는 양수를 최대 255까지만 담아서(부호비트 1개를 뺏기니까), 256에서 음수로
넘쳐(overflow) "지도 밖"으로 잘못 판정됨. **해결**: 정수부를 10비트로 늘림
(부호있는 10비트 = 양수 최대 511, 트랙 폭 400을 여유있게 커버). 재컴파일 후
**6/6 완전 통과.**

**검증 결과**: 트랙 중앙(200,80)에서 +x 119칸, -x 18칸, +y 16칸, -y 28칸,
대각선 33칸, 그리고 일부러 벽 바로 앞(10,80)에서 쏴서 0칸(즉시 충돌) 확인까지
— 소프트웨어 정답과 RTL이 전부 정확히 일치.

**남은 것**:
- 거리장 기반 마칭(빈 공간에서 큰 보폭) — 지금은 실제 트랙에서도 여전히 1칸씩만 감(119번 반복 등, 느림)
- 지도를 32비트 워드로 묶어 BRAM 효율 높이기(지금은 셀당 1비트 그대로 씀)
- v1(센서모델)과 v2(레이마칭) 결합 — ray_march_bram의 dist_o를 sensor_pe의 addr_gen 입력(exp_range)으로 바로 연결
- Jetson 실측, 보드 확정 후 합성 — 계속 보류

## 2026-09-02 (이어서) — 감사(ponytail-audit) + v3: v1(센서모델)+v2(레이마칭) 결합

**감사**: ponytail-audit 스킬로 전체 코드 훑음. 발견: table_mem.v/ray_march.v가
각각 table_mem_dp.v/ray_march_bram.v의 하위호환(포트0개/작은지도 버전)이라 기계적
으로는 삭제 가능 — 근데 "작은 것부터 실제 스펙까지 키워온 과정"을 문서화해둔
가치가 있어서 사용자 판단으로 남겨두기로 함. 트레이드오프 없는 것 두 개만 반영:
- ray_march.v/ray_march_bram.v의 DIST_W(사람이 MAX_STEPS와 손으로 맞춰야 했음)를
  `$clog2(MAX_STEPS+1)`로 자동계산 — 동기화 깜빡하면 조용히 깨지는 위험 제거.
- gen_sensor_model.py의 to_fixed_q(signed=True) — 호출부 1곳뿐이고 항상 기본값만
  씀, unsigned 죽은 분기 제거.
회귀 전부 통과 확인 후 커밋.

**v3: particle_scorer.v** — v1(센서모델 PE)과 v2(레이마칭)를 처음으로 실제 연결.
빔 하나마다: ①레이마칭으로 기대거리(d) 구하기 → ②관측거리(r)와 함께 addr_gen으로
주소 계산 → ③센서모델 PE에 한 클럭 먹여서 누적, 이걸 빔 개수만큼 반복해서 파티클
점수를 낸다 — 실제 particle_filter.py의 sensor_model() 한 사이클 전체를 하드웨어로
옮긴 것. 조율 FSM(6상태: IDLE/WAIT_BEAM/MARCH/SCORE/BEAM_DONE/WAIT_PE)이 서브모듈
4개(ray_march_bram+addr_gen+table_mem+sensor_pe)를 지휘.

**테스트 정답지**: `echo-ref/gen_particle_scorer_test.py` — gen_sensor_model.py/
gen_track_map.py를 그대로 import(재구현 아니라 재사용). changwon 트랙 자유공간
한 점(자동 탐색)에서 부채꼴 8방향(-60°~+60°)으로 레이마칭 → 기대거리에 노이즈
얹어 관측거리 생성 → 양자화 테이블로 최종 파티클 점수까지 계산.

**결과: 첫 시도에 PASS.** weight_o=-7441, 정답과 정확히 일치. 이전 단계들에서
겪었던 반올림 버그·비트폭 버그 같은 게 이번엔 없었음(이미 검증된 조각들을
올바른 순서로 이어붙이기만 했기 때문으로 보임).

**부수 관찰(진짜 의미있는 확인)**: 빔별 처리시간 로그 — 거리 24~34칸짜리 빔들은
빨리 끝나는데, 거리 119칸짜리 빔(4번) 하나가 다른 빔들 다 합친 것보다 오래 걸림
(빔3→4 구간만 1,260,000ns, 다른 구간은 300~600,000ns대). **"레이마칭이 전체
비용의 90%"라던 이 프로젝트의 최초 실측 결과가, 이제 RTL 시뮬레이션 타이밍
에서도 똑같이 확인됨** — 처음 문제정의부터 지금까지 일관된 결론.

**남은 것**:
- 거리장 기반 마칭 최적화(지금도 여전히 1칸씩 걸음)
- 파티클 여러 개 동시 처리(v1에서 했던 PE 병렬화를 particle_scorer 단위로)
- 지도를 32비트 워드로 묶어 BRAM 효율 높이기
- Jetson 실측, 보드 확정 후 합성 — 계속 보류

## 2026-09-02 (이어서) — 파티클 여러 개 동시 처리(particle_scorer 병렬화)

**v1(센서모델 PE)에서 검증한 병렬화 패턴을 v3(전체 파이프라인) 단위로 반복.**
particle_scorer.v가 이미 내부에 ray_march_bram+addr_gen+table_mem+sensor_pe를
다 갖춘 독립 블록이라, 2개를 나란히 놓기만 하면 됨(추가 배선 거의 없음).

**테스트 데이터**: gen_particle_scorer_test.py를 다중 파티클 지원으로 확장 —
changwon 트랙에서 서로 다른 자유공간 3곳(파티클0/1/2용, 이번엔 0/1만 씀)을 찾아
각각 부채꼴 8빔 시나리오+정답 생성. 파티클0은 이전 세션과 동일 시작점(x0=200,
y0=80)이라 정답도 그대로(-7441)임을 재확인 — 재현성 체크 겸함.

**tb_particle_scorer_parallel.v**: particle_scorer 2개, task 2개(drive_particle0/1,
각각 한 번씩만 호출 — tb_sensor_pe_x4.v에서 겪었던 "task를 fork로 동시에 여러 번
부르면 재진입성 버그가 난다"는 교훈을 아예 문제가 생길 수 없는 구조로 회피).

**결과: 첫 시도에 PASS.**
- scorer0: -7441 (정답 일치), scorer1: -6687 (정답 일치)
- **총 소요시간 4300ns — 단일 particle_scorer 처리시간(4305ns)과 거의 동일.**
  즉 파티클 2개를 레이마칭부터 센서모델 채점까지 **전체 파이프라인 단위로 완전히
  동시 처리**했다는 뜻 — v1의 "PE 4배 병렬" 스토리가 이번엔 훨씬 무거운(레이마칭
  포함) 단위에서 그대로 재현됨.

**남은 것**:
- 파티클을 4개, 8개로 더 늘리기 — v1 때처럼 지도/테이블 복사본 개수 vs BRAM
  예산 트레이드오프를 다시 고민해야 함(이번엔 지도+테이블 둘 다 복제해야 해서
  v1보다 자원 비용이 더 큼 — 파티클당 map(~10KB)+table(~144KB)≈154KB)
- 거리장 기반 마칭 최적화(지금도 여전히 1칸씩 걸음)
- 지도를 32비트 워드로 묶어 BRAM 효율 높이기
- Jetson 실측, 보드 확정 후 합성 — 계속 보류

## 2026-09-02 (이어서) — v2.2: 거리장(EDT) 기반 마칭 최적화

**문제**: ray_march_bram.v(v2.1)는 여전히 1칸씩만 걸어서, 트랙 중앙에서 먼 벽까지
119칸이면 119번(+오버헤드) 반복해야 함 — "레이마칭이 전체 비용의 90%"라는 실측
결과의 핵심 원인이 바로 이 "느린 걸음"이었음.

**설계 — 곱셈기 딜레마**: 진짜 range_libc의 마칭처럼 "빈 공간에서 성큼성큼" 걸으려면
매 스텝 "가장 가까운 벽까지 안전거리"만큼 방향벡터를 스케일해서 전진해야 하는데,
이 안전거리는 지도에서 그때그때 읽어오는 **런타임 값**이라(301 같은 컴파일타임
상수가 아님) 진짜 곱셈기 없이는 못 만듦. **절충안**: 안전거리를 "2의 거듭제곱 중
그 이하인 가장 큰 값"으로 반올림 → 곱셈이 **가변 시프트(배럴 시프터)**로 바뀜.
최적 보폭보다 살짝 작게 걷는 손해가 있지만, 지금까지 지켜온 "곱셈기 회피" 원칙을
그대로 유지하면서 큰 성능 개선을 얻음.

**구현**:
- `echo-ref/gen_track_map.py`: `compute_edt_shift()` — scipy의
  `distance_transform_edt`(lattice_planner.cpp의 buildEDT와 같은 개념, Felzenszwalb
  EDT)로 자유공간 각 칸의 "벽까지 거리"를 구하고, 2의 거듭제곱 중 이하 최대값의
  지수(0~7)를 저장. `march_edt()` — 파이썬으로 먼저 검증(같은 6개 시나리오,
  **이동거리는 기존과 정확히 동일, 반복 횟수는 2.7~7.5배 감소**).
- `rtl/ray_march_edt.v` — ray_march_bram.v와 인터페이스 동일, 내부만 다름: 매
  스텝 `x <= x + (dx <<< shift); dist_acc <= dist_acc + (1 <<< shift);` — 두
  개(방향벡터, 거리누적) 다 같은 시프트 값으로 한 번에 전진.

**검증**: `tb/tb_ray_march_edt.v`, 같은 6케이스 — **6/6 PASS, 이동거리는 기존과
100% 일치(당연히 같은 물리 거리), 클럭 수는 실측으로 최대 6배 이상 감소**
(center_+x: 120→19클럭, center_-y: 29→9클럭 등). 파이썬 예측(2.7~7.5배)과
같은 방향, 정확한 배수는 측정 오버헤드(시작/종료 클럭 계산 방식 차이) 때문에
약간 다름 — 핵심(이동거리 정확·클럭 수 확실히 감소)은 명확히 확인됨.

**남은 것**:
- particle_scorer.v를 ray_march_edt 버전으로 교체(지금은 여전히 ray_march_bram 씀)
- 파티클을 4개, 8개로 더 늘리기
- 지도를 32비트 워드로 묶어 BRAM 효율 높이기
- Jetson 실측, 보드 확정 후 합성 — 계속 보류

## 2026-09-02 (이어서) — particle_scorer를 거리장 기반 레이마칭으로 교체

새로 만든 ray_march_edt.v를 particle_scorer.v 내부에 그대로 꽂아넣음(인터페이스가
ray_march_bram.v와 완전히 같아서 딱 한 줄만 바꾸면 됨 — 모듈을 독립적으로 잘
설계해둔 덕을 봄).

결과: 정확성 그대로 유지(단일 -7441, 병렬 -7441/-6687 전부 일치), 속도는:
- 단일 particle_scorer: 4305ns -> 1315ns (약 3.3배)
- 2개 병렬: 1310ns (단일 처리와 거의 동일 -> 최적화 효과와 병렬화 효과가 서로
  방해 없이 같이 살아있음을 확인)

이걸로 ECHO의 두 최적화 기법(병렬화, 거리장 마칭)이 서로 독립적으로 조합 가능함을
실측으로 확인함 — 이게 사실 좋은 설계의 증거: 모듈을 서로 낮은 결합도로 짜뒀기
때문에 한쪽을 바꿔도 다른 쪽 특성이 안 깨짐.

**남은 것**:
- 파티클을 4개, 8개로 더 늘리기
- 지도를 32비트 워드로 묶어 BRAM 효율 높이기
- Jetson 실측, 보드 확정 후 합성 — 계속 보류

## 2026-09-02 (이어서2) — v3를 파티클 4개 병렬로 확장 + 정답지(오라클) 버그 발견·수정

**목표**: v1(sensor_pe)에서 이미 검증한 "PE 2개 -> 4개 병렬" 패턴을 v3(particle_scorer,
레이마칭+센서모델 결합)에도 그대로 적용. particle_scorer.v가 내부에 필요한 서브모듈을
전부 갖춘 독립 블록이라 4개를 나란히 놓기만 하면 됨 — `tb_particle_scorer_x4.v` 신규
(tb_particle_scorer_parallel.v의 2개 버전을 그대로 4개로 늘림, named task 4개).

**정답지도 파티클 4개로 확장하며 발견한 버그**: 기존 `SEARCH_CENTERS`가 2~3개뿐이라
4번째 파티클 시작점(x0=150,y0=140)을 추가하고 오라클(`gen_particle_scorer_test.py`)을
다시 돌렸더니, 파티클 2번(x0=320,y0=120)에서 RTL과 오라클이 처음으로 어긋남
(RTL weight=-7472, 오라클 기대값=-7466) — 이전까지 검증한 파티클 0/1은 우연히
어긋나는 지점을 안 지나갔던 것으로 드러남.

**원인 추적**: `ray_march_edt`를 단독으로 파티클2의 빔 8개에 직접 흘려서 빔별
dist를 찍어보니, 빔 0(각도 -60도)만 RTL dist=15, 오라클 d=16으로 1칸 어긋남
(나머지 7개는 정확히 일치). 오라클(`score_one_particle`)은 `math.cos`/`math.sin`
정밀 float 값을 그대로 `march_edt()`에 넣어 d를 구하는데, RTL은 이 방향벡터를
Q9.8로 양자화한 값만 받는다 — 벽 여유가 딱 1칸 안쪽인 코너 케이스에서는 이
미세한 방향 차이만으로도 서로 다른 칸에서 충돌을 감지해 d가 갈릴 수 있음(실측
확인). v1 때 겪었던 "파이썬이 float로 합산 후 반올림 vs RTL이 반올림된 값들을
합산"과 완전히 같은 종류의 문제 — 이번엔 산술이 아니라 기하(방향벡터)에서 재발.

**수정**: `quantize(v, scale)` 헬퍼 추가 — RTL이 실제로 받을 고정소수점 값으로
먼저 반올림한 뒤, 그 양자화된 dx/dy로 `march_edt()`를 호출하도록 `score_one_particle`
수정. 이후 파티클2 빔0도 RTL과 동일하게 d=15로 나오고, 전체 4파티클 재검증 통과.

**검증 결과**: `tb_particle_scorer_x4.v` — 4/4 PASS
(-7441/-6687/-7462/-7623), 총 소요시간 1520ns(단일 EDT 처리 1315ns와 비슷한
수준 — v1 4배 병렬 때와 같은 패턴으로 "진짜 병렬" 확인, 파티클마다 빔 거리
분포가 달라 정확히 같은 숫자는 아님).

**리소스 비용 확정**: particle_scorer 인스턴스 하나가 지도 BRAM(occupancy+EDT
shift) + 센서모델 테이블 BRAM을 전부 자기 것으로 따로 인스턴스화함(포트/BRAM
공유 없음) — 4개 병렬 = 위 세 BRAM을 4벌 복사하는 비용. 정확한 용량은 아직 실측
안 함(합성 전이라 유틸리제이션 리포트 없음), 다음에 필요하면 실측.

**남은 것**:
- 지도를 32비트 워드로 묶어 BRAM 효율 높이기(파티클 4개 이상으로 늘릴수록
  BRAM 낭비 비용이 커지니 우선순위 올라감)
- Jetson 실측, 보드 확정 후 합성 — 계속 보류

## 2026-09-02 (이어서3) — 첫 실제 합성(Vivado, 보드 없이 무료로) 완료

**배경**: 실물 FPGA 보드를 살 돈이 없다는 제약 확인 → 대신 **Vivado ML Standard
Edition**(완전 무료, Artix-7/Zynq-7000 계열 무라이선스 지원 확인)로 "보드 없이
합성/타이밍/자원 리포트까지"를 목표로 잡음. 오늘 설치한 Vivado 2024.1(Windows
쪽, `C:\Xilinx\Vivado\2024.1`)이 이미 있음을 확인, `artix7` 디바이스 파일도
설치돼있어 추가 다운로드 없이 바로 시작 가능했음.

**환경 버그 2종 발견·해결(재발 방지용 기록)**:
1. **작업 디렉터리 경로에 한글이 있으면 Vivado가 크래시함.**
   `C:\SNU\포트폴리오\ZERO\echo-synth`에서 첫 시도 시 `sensor_pe` 모듈 합성까지는
   성공했는데 그 직후 `TclStackFree: incorrect freePtr... Abnormal program
   termination`으로 죽음 — 로그의 경로가 `C:/SNU/��Ʈ������/ZERO/...`로 깨져
   찍힌 걸 보고 원인 확정(Vivado 내부 Tcl이 비-ASCII 경로를 못 다룸). **순수
   영문 경로(`C:\Users\ggang\echo-synth`)로 작업 디렉터리를 옮겨서 해결.**
2. **설치 직후 일부 Vivado 내부 파일이 클라우드 플레이스홀더 상태(`RecallOnOpen`
   속성)라 처음 접근 시 `couldn't read file ...: No error`로 실패함**
   (`unimacro_verilog.tcl`, BRAM 추론에 필요한 라이브러리 파일). PowerShell로
   그 파일을 한 번 직접 읽어(`Get-Content`) 로컬에 내려받히게 한 뒤 재시도하니
   해결됨 — 오늘 막 설치된(50GB+) 파일들이 어떤 백업/동기화 에이전트에 의해
   지연 다운로드 상태로 남아있었던 것으로 추정.

**타깃**: Arty A7-35T의 `xc7a35ticsg324-1L` — 무료 라이선스 대상, 실물은 없지만
합성/타이밍 계산기로서는 정확한 실제 칩 스펙을 씀.

**sanity 합성** (`sensor_pe.v` 단독, 메모리 없음): 파이프라인 자체 검증용,
144셀(FDCE 23개 등)로 매핑, 에러 0.

**본 목표: `particle_scorer.v`(v3 전체) 합성 성공.** 5개 RTL 파일
(`ray_march_edt.v`, `addr_gen.v`, `table_mem.v`, `sensor_pe.v`, `particle_scorer.v`)
+ changwon 지도/EDT/센서모델 `.hex` 3개를 그대로 읽혀서 합성, **에러 0개.**

**실측 자원 사용량** (파티클 1개분, `util_v3_timed.rpt`):
| 자원 | 사용 | 전체 | 비율 |
|---|---|---|---|
| Slice LUT | 1332 | 20800 | 6.40% |
| Slice Register | 253 | 41600 | 0.61% |
| Block RAM(RAMB36) | **39** | **50** | **78.00%** |
| DSP | **0** | 90 | **0.00%** |

**핵심 발견 1 — BRAM이 진짜 병목, 시뮬로는 절대 몰랐을 정보.** particle_scorer
하나가 이미 이 칩 BRAM의 78%를 씀 — **2개 병렬(78%×2>100%)조차 이 칩에는 물리적으로
안 들어감.** 시뮬레이션에서 검증한 "4개 병렬"은 어디까지나 기능 검증이었고, 실제
칩에 넣으려면 (a) 더 큰 칩(BRAM 많은 것) 또는 (b) 맵 데이터를 32비트 워드로
패킹(README/progress에 이미 남아있던 과제, 지금 이 실측으로 우선순위가 확정됨)이
필요함을 실제 숫자로 확인.

**핵심 발견 2 — DSP 0개, "곱셈기 없는 설계" 원칙이 합성 결과로 실증됨.** 이번
학기 내내 log-domain 누산(v1), 시프트-덧셈 주소생성, 배럴시프터 EDT 마칭(v2)
전부 "곱셈기 회피"로 설계했는데, 실제 합성기가 DSP(하드 곱셈기 블록)를 단 하나도
안 썼다는 게 처음으로 실물 증거로 확인됨.

**타이밍 (100MHz/10ns 클럭 제약, `constraints.xdc`)**: WNS=-2.594ns(제약
위반) → **실제 달성 가능한 최대 클럭 ≈ 1/(10+2.594)ns ≈ 79.4MHz.**
크리티컬 패스(`timing_v3_worstpath.rpt`, 로직레벨 12단, `ray_march_edt/x_reg`
갱신 경로) — `step_dx = dx <<< cur_shift`(가변 배럴 시프터, `cur_shift`가
BRAM에서 매 스텝 읽어온 런타임 값이라 시프트량이 고정 안 됨)가 병목으로 확정.
곱셈기 없이 설계한 대가(가변 시프트 자체가 넓은 mux 트리로 합성됨)가 타이밍
크리티컬 패스로 정확히 나타남 — 다음 최적화 타깃.

**결론**: `synth/`에 재현 가능한 스크립트+리포트 커밋. "논문 수준" 대비 완성도
평가(이전 세션 논의)에서 가장 컸던 "합성/실측 0%" 격차를 상당 부분 메움 — 실물
보드 실행(진짜 클럭에서 electrons 흐르는 것) 없이도 "실제 칩에 들어가는지",
"진짜 자원을 얼마나 쓰는지", "진짜 최대 속도가 얼마인지"는 전부 답할 수 있게 됨.

**남은 것**:
- 크리티컬 패스(배럴 시프터) 최적화 — 예: `cur_shift` 룩업과 시프트 사이에
  파이프라인 레지스터 삽입해서 로직레벨 줄이기
- BRAM 78% 문제 해결 — 맵 32비트 워드 패킹이 최우선 후보
- Zynq-7000(더 큰 BRAM) 등 다른 무료 지원 디바이스로도 합성해서 비교
- place_design/route_design까지 가면 더 정확한(포스트-라우트) 타이밍 확보 가능 —
  지금은 synth-only(포스트-신스) 추정치

## 2026-09-02 (이어서4) — BRAM 병목 해결: 다른 프로젝트(AER 중재기) 재사용으로 파티클 2개를 BRAM 추가비용 0으로 합성

**출발점**: 직전 합성 실측에서 BRAM 78%(39/50)가 전부 `table_mem`(센서모델
로그확률 테이블) 하나 때문임을 계층별 리포트(`util_v3_particle_scorer_hier.rpt`)
로 확정(레이마칭/주소생성/PE는 BRAM 0). 즉 파티클을 늘리려면 이 테이블 하나를
공유해야 함.

**1차 시도(실패) — `table_mem_dp.v`로 진짜 듀얼포트 공유**: v1(`sensor_pe`)에서
이미 시뮬레이션으로 검증됐던 패턴을 그대로 `particle_scorer`에 적용
(`particle_scorer_shared.v` + `particle_scorer_pair.v`, 테이블을 외부 포트로
빼고 `table_mem_dp` 하나를 2개 스코어러가 공유). 시뮬레이션 기능검증은
통과했지만(`-7441`/`-6687`, 1310ns, 기존과 동일) **실제 합성에서 BRAM이
156/100으로 초과(예산의 1.56배), LUT-RAM 폴백 시도 중 사실상 진행이 멈춰서
강제 종료함.** 원인: 90601깊이(13비트폭) 테이블은 BRAM 블록 ~39개를 이어붙여야
하는데, 이 전체 깊이에 대해 두 포트가 각자 아무 주소나 자유롭게 접근하는
"진짜 크로스바"를 합성기가 못 만들고 통째로 복사해버림 — **"시뮬레이션에서
검증된 패턴"이 "합성 가능"과 동의어가 아님을 실측으로 처음 확인한 사례.**
`particle_scorer_shared.v`/`particle_scorer_pair.v`는 실패 사례로 코드에 남겨둠
(재시도 방지용 기록).

**2차 시도(성공) — 사용자의 다른 프로젝트(`전국AI반도체경진대회`, AER 이벤트
중재기 설계)에서 `arbiter2.v`를 그대로 재사용.** 진짜 듀얼포트 대신, single-port
`table_mem` 하나를 2-input round-robin 중재기로 매 클럭 시분할하는 구조로
재설계(`particle_scorer_arb.v` + `particle_scorer_pair_arb.v`). 사용자가 같은
프로젝트의 더 정교한 `cluster2_steal_buf`(수십~수백 개의 스파스/불균등 이벤트
소스 처리량 최적화용)도 있다고 언급했으나, 우리 문제는 요청자가 딱 2개고 요청
간격도 넉넉해서(레이마칭이 빔당 수십 클럭) `steal_buf`의 최적화가 적용될 여지가
없음 — 가장 작은 원시 모듈(`arbiter2`)이 정확히 문제 크기에 맞는 선택으로 판단,
그대로 진행.

**중재 도입 중 실제로 찾은 타이밍 버그**: 1차 구현(그냥 req/gnt를 매 클럭
그대로 중재기에 넣고 승인되면 바로 `pe_valid` 발사)은 기능검증 자체가 실패함
(`weight0=-7459`(기대 `-7441`), `weight1=-10357`(기대 `-6687`)). 원인 추적:
`sensor_pe.v`는 "`valid_i` 준 지 1클럭 뒤 `table_data` 사용"이라고 문서화돼
있지만, 내부에 `pend_valid` 릴레이 단이 하나 더 있어서 실제로는 **승인 클럭+
그 다음 클럭까지 2클럭 동안 테이블 주소가 안정적으로 유지돼야** 정확히 동작함
— private(전용) 메모리에서는 주소가 원래 안 바뀌니 이 여유 요구사항이 한 번도
드러난 적이 없었음. 공유 환경에서는 승인 직후 상대방이 바로 다음 클럭에
포트를 채가면서(각자 요청은 계속 매 클럭 재평가되므로) 테이블 출력 레지스터가
덮어써져 사라짐 — 클럭 단위로 직접 파형을 추적해서 확정. **수정**: 승인 후
1클럭 동안 새 중재를 막고(`busy`) 같은 승자의 주소를 그대로 붙잡아두는 홀드
레지스터(`held_sel`)를 `particle_scorer_pair_arb.v`에 추가(`arbiter2.v` 원본은
안 건드림 — 감싸는 방식으로만 수정). 재검증: `tb_particle_scorer_pair_arb.v`
2/2 PASS(`-7441`,`-6687`), 1310ns(공유 안 했을 때와 동일 — 이 테스트 규모에서는
포트 경합이 사실상 없었다는 뜻).

**합성 실측 (`util_pair_arb.rpt`, `util_pair_arb_hier.rpt`)**:
| 자원 | 파티클 1개(기존) | 파티클 2개(중재 공유) |
|---|---|---|
| LUT | 6.40%(1332) | 13.07%(2719) |
| FF | 0.61%(253) | 0.91%(378) |
| BRAM | **78.00%(39/50)** | **78.00%(39/50) — 동일!** |
| DSP | 0% | 0% |

계층별 리포트로 `u_table_shared`(테이블 하나)가 BRAM 39/39(100%)를 전부 차지,
`u_arb`(중재기)는 LUT 2개·FF 1개로 사실상 공짜임을 확인. **타이밍도
WNS=-2.594ns로 파티클 1개 때와 완전히 동일**(`timing_pair_arb.rpt`) — 중재
로직이 크리티컬 패스(여전히 `ray_march_edt` 배럴 시프터)를 악화시키지 않음,
실제 최대 클럭 그대로 ≈79MHz.

**결론**: BRAM 추가비용 0으로 파티클을 2배로 늘릴 수 있음을 실제 합성으로
증명. arbiter2가 4-input(`arbiter4.v`, 같은 AER 프로젝트에 이미 있음)으로
확장되면 이론상 파티클 4개까지 같은 테이블 하나 공유로 늘릴 수 있을 것으로
보이나(경합 확률과 대기 오버헤드는 커짐), 아직 미시도.

**이번에 건드린 파일**: `rtl/particle_scorer_shared.v`,
`rtl/particle_scorer_pair.v`(둘 다 실패 사례로 보존), `rtl/particle_scorer_arb.v`,
`rtl/particle_scorer_pair_arb.v`(최종), `rtl/arbiter2.v`(전국AI반도체경진대회
프로젝트에서 그대로 복사, 무수정), `tb/tb_particle_scorer_pair.v`,
`tb/tb_particle_scorer_pair_arb.v`, `synth/synth_pair_arb.tcl` +
`synth/util_pair_arb*.rpt`/`synth/timing_pair_arb.rpt`.

**남은 것**:
- `arbiter4.v` 재사용해서 파티클 4개가 테이블 하나 공유하는 버전 시도(경합
  증가로 인한 처리량 저하 실측 필요)
- 크리티컬 패스(배럴 시프터) 최적화는 여전히 미착수
- place_design/route_design까지 가면 포스트-라우트 정확한 타이밍 확보 가능

## 2026-09-02 (이어서5) — arbiter4로 파티클 4개까지 확장, BRAM 여전히 동일

**목표**: 직전 arbiter2 성공(파티클 2개, BRAM 39/50 유지)을 4개로 확장. 같은
`전국AI반도체경진대회` 프로젝트에 이미 있던 `arbiter4.v`(4-input round-robin,
req/gnt 4비트, 우선순위가 마지막 승자 다음으로 회전하는 방식이라 특정 요청자가
계속 굶는 걸 방지)를 그대로 재사용.

**구현**: `particle_scorer_quad_arb.v` — `particle_scorer_pair_arb.v`의 구조를
그대로 4개로 확장. 승인 후 1클럭 더 주소를 붙잡아두는 홀드 로직(`busy`/
`held_sel`, pair 버전에서 실측으로 확정했던 그 타이밍 요구사항)을 1비트→2비트
인덱스로만 일반화, 나머지 구조(4-way 주소 mux, 공유 테이블 하나)는 pair 버전과
동일한 원리를 그대로 복제.

**기능 검증**: `tb_particle_scorer_quad_arb.v` — particle0~3 정답지(이미
sim/에 있던 것, 4파티클 병렬 확장 때 만든 것 재사용) 그대로 사용, **4/4 PASS
한 번에 통과**(pair 버전에서 이미 타이밍 버그를 잡아놓은 덕에 추가 디버깅
없었음). 총 소요시간 1540ns — 독립 테이블 4배 병렬 버전(1520ns)과 거의 동일
(+1.3%), 4-way 시분할이어도 실제 포트 경합이 거의 발생하지 않았다는 뜻.

**환경 이슈 재발**: 합성 중 `unimacro_verilog.tcl` "클라우드 플레이스홀더"
오류가 다시 발생함(2026-09-02 앞선 세션에서 이미 한 번 겪었던 것과 동일) —
한 번 읽어서 하이드레이션된 파일도 시간이 지나면(또는 다른 프로세스가) 다시
플레이스홀더 상태로 되돌아갈 수 있다는 뜻으로 보임. 같은 워크어라운드
(PowerShell `Get-Content`로 재하이드레이션)로 즉시 해결됨 — 원인 미상이지만
재발 가능성 있는 걸로 기록, 합성 실패 시 제일 먼저 의심할 것.

**합성 실측 (`util_quad_arb.rpt`)**:
| 자원 | 파티클 1개 | 파티클 2개 | 파티클 4개 |
|---|---|---|---|
| LUT | 6.40% | 13.07% | 25.88%(5384) |
| FF | 0.61% | 0.91% | 1.80%(749) |
| BRAM | 78.00%(39) | 78.00%(39) | **78.00%(39) — 여전히 동일** |
| DSP | 0% | 0% | 0% |

타이밍도 WNS=-2.594ns(≈79MHz)로 파티클 1/2개 때와 완전히 동일 — 크리티컬
패스(`ray_march_edt` 배럴 시프터)가 중재 로직 추가와 무관하게 그대로 병목.

**결론**: 이 칩(XC7A35T, 무료 라이선스) 하나로 particle_scorer **1→4개를
BRAM 추가비용 0으로 전부 커버함을 실제 합성으로 확정.** LUT 25.88%로 여유도
충분해서(8개까지도 LUT만 보면 이론상 여지 있음, 미시도) 다음 단계로 8개
시도도 가능해 보임 — 다만 실제 병목이 BRAM에서 LUT/타이밍(크리티컬 패스)으로
넘어갈 가능성 있음.

**이번에 건드린 파일**: `rtl/particle_scorer_quad_arb.v`, `rtl/arbiter4.v`
(전국AI반도체경진대회 프로젝트에서 그대로 복사), `tb/tb_particle_scorer_quad_arb.v`,
`synth/synth_quad_arb.tcl` + `synth/util_quad_arb*.rpt`/`synth/timing_quad_arb.rpt`.

**남은 것**:
- 8파티클 시도(LUT는 여유 있어 보이나 크리티컬 패스/타이밍이 어떻게 되는지
  미지수)
- 크리티컬 패스(배럴 시프터) 최적화는 여전히 미착수 — 세 번 다 똑같은
  -2.594ns로 나온 걸로 봐서 다음 우선순위로 올려도 될 듯
- place_design/route_design까지 가면 포스트-라우트 정확한 타이밍 확보 가능

## 2026-09-02 (이어서6) — arbiter8로 파티클 8개까지 확장, BRAM 여전히 39/50

**구현**: `particle_scorer_oct_arb.v` — quad_arb 구조를 그대로 8-way로 확장,
`arbiter8.v`(같은 전국AI반도체경진대회 프로젝트)를 재사용. 홀드 로직
(busy/held_sel)을 3비트 인덱스로 일반화. 테스트벡터는 `gen_particle_scorer_test.py`의
`SEARCH_CENTERS`를 4개→8개로 늘려서 재생성(particle4~7 신규).

**테스트벤치 작성 중 발견한 Icarus 제약**: 처음엔 `reg [..] dxs[0:7][0:7]`처럼
2차원 메모리 배열로 8명분을 묶어 반복문으로 로드하려 했으나, Icarus의
`$readmemh`가 `dxs[idx]`(부분 인덱싱으로 얻어지는 내부 1차원 슬라이스)를
대상으로 못 받음("needs 2 indices, but got only 1" 에러) — pair/quad와 같은
완전 명시(파티클마다 별도 이름의 1차원 배열 8벌) 스타일로 되돌림. 장황하지만
확실히 동작하는 패턴을 우선시함(이 프로젝트 전체의 반복된 선택).

**기능 검증**: `tb_particle_scorer_oct_arb.v` — 8/8 PASS(-7441,-6687,-7462,
-7623,-7039,-7256,-7017,-6791), 1660ns(독립 4배 병렬의 1520ns 대비 +9.2%,
2배 더 많은 요청자가 경합하니 지연이 조금 더 늘었지만 여전히 작음).

**합성 실측 (`util_oct_arb.rpt`)**: BRAM **39/50(78%) — 파티클 1/2/4개 때와
완전히 동일.** LUT 51.77%(10769/20800, 로직 8배 — 4개 때의 25.88%에서
정확히 약 2배), FF 3.62%(1507), DSP 여전히 0%. **타이밍은 WNS=-2.438ns로
1~4개 때의 -2.594ns와 소폭 다름**(≈80.4MHz vs ≈79.4MHz, 사실상 비슷하지만
"완전히 동일"은 아님 — 정직하게 구분해서 기록, 최악경로가 여전히
`ray_march_edt` 배럴 시프터인지는 이번엔 상세 리포트로 재확인 안 함, 다음
과제).

**환경 이슈**: 합성 시작 전에 `unimacro_verilog.tcl`을 미리 한 번 읽어
하이드레이션해뒀더니 이번엔 클라우드 플레이스홀더 오류 재발 없이 한 번에
성공 — 예방적 워크어라운드로 굳어짐(다음에도 합성 전에 항상 먼저 이 파일을
한 번 읽을 것).

**결론**: 이 칩(XC7A35T) 하나로 파티클 **1→8개**를 BRAM 추가비용 0으로 전부
커버함을 확정. LUT가 51.77%까지 올라와서, 이 이상(16개 등) 늘리면 LUT가
먼저 병목이 될 가능성이 보임 — BRAM 공유 자체는 이제 한계에 다다른 게
아니라 다른 자원(LUT/타이밍)으로 병목이 넘어가는 국면.

## README 재구성 (사용자 피드백 반영)

사용자가 "2분만 보고 이해하는 사람" 기준으로 README 개선 요청 — 4가지 반영:
1. 도입부를 "v2 진행중"(stale) → 현재 실제 상태(v3.3/3.4, 합성 완료, BRAM
   공유 성공)로 정정
2. 파이프라인 다이어그램(파티클+라이다 → ray_march_edt → addr_gen → arbiter
   → table_mem → sensor_pe) 신규 추가
3. 수치 결과 표(구성별 BRAM/LUT/DSP/타이밍/지연시간) 신규 추가 — 병목이
   BRAM이었고 중재로 해소됐다는 걸 표 하나로 보이게
4. "빠른 시작" 섹션 신규 — 최신 마일스톤(quad_arb) 시뮬 명령을 맨 앞으로,
   합성 재현 방법과 한글경로 주의사항도 포함
5. (추가로 발견) `tools/gen_particle_scorer_test.py` 등 3개 스크립트는 이미
   `tools/`에 커밋돼 있었음(이번 세션 중 계속 동기화해왔음) — README 문구가
   "Windows echo-ref에만 있다"고 stale하게 남아있던 것만 정정, 실제 파일
   이동 작업은 필요 없었음
progress.md는 기존처럼 상세 연구노트로 유지, README만 앞부분 재구성.

**이번에 건드린 파일**: `rtl/particle_scorer_oct_arb.v`, `rtl/arbiter8.v`
(전국AI반도체경진대회 프로젝트에서 그대로 복사), `tb/tb_particle_scorer_oct_arb.v`,
`sim/particle{4,5,6,7}_*.hex`(신규), `tools/gen_particle_scorer_test.py`
(SEARCH_CENTERS 4→8개), `synth/synth_oct_arb.tcl` +
`synth/util_oct_arb*.rpt`/`synth/timing_oct_arb.rpt`, `README.md`(재구성).

**남은 것**:
- 8개 결과의 최악경로 상세(`report_timing -delay_type max`)로 크리티컬 패스
  재확인 — WNS이 달라진 이유 규명
- 16개까지 더 늘려서 LUT가 진짜 병목이 되는 지점 찾기(미시도)
- 크리티컬 패스(배럴 시프터) 최적화는 여전히 미착수
- place_design/route_design까지 가면 포스트-라우트 정확한 타이밍 확보 가능

## 2026-09-02 (이어서7) — 8-way 타이밍 차이 원인 확인: 새 병목 아님

`timing_oct_worstpath.rpt`(report_timing -delay_type max)로 확인 — 8파티클의
크리티컬 패스도 여전히 u_scorer0/u_ray_march(ray_march_edt)의 배럴 시프터
(y_reg->x_reg 경로), 1개 때와 동일한 병목. 다만 로직레벨이 12단(CARRY4x2,
LUT5x1, LUT6x9)에서 13단(CARRY4x3, LUT3x1, LUT6x8, MUXF7x1)으로 매핑이
달라졌고, 데이터 경로 지연은 오히려 12.168ns->12.012ns로 0.156ns 더
빨라짐(WNS 차이 -2.594 vs -2.438과 정확히 일치) — 설계 규모가 8배로
커지면서 합성기가 같은 로직을 미세하게 다른 게이트 조합으로 매핑한 것뿐,
새로운 병목이 생긴 게 아님을 확정. README의 "8-way WNS가 소폭 다름" 각주에
대한 후속 확인 완료.

**이번에 건드린 파일**: `synth/synth_oct_worstpath.tcl`, `synth/timing_oct_worstpath.rpt`.

## 2026-09-02 (이어서8) — 첫 post-route 실측 + 크리티컬 패스 파이프라인 시도는 계산으로 기각

**사용자 피드백으로 방향 전환**: "FPGA가 실제 ZERO particle_filter보다 확실히
빠른가?"라는 질문에 대해, 지금까지의 성과(1→8파티클 BRAM 공유 성공)와 실제
ZERO 대체 가능성 사이의 큰 격차를 사용자가 직접 정리해줌 — 반영 내용은
`docs/problem_statement.md`의 "2026-09-02 추가" 섹션 참고(빔 8개 vs 60개,
입력형태 불일치, 파티클 규모 8 vs 500~4000, 엔드투엔드 실측 없음, 정확도
비교 없음). 다음 우선순위를 "성능 최적화"에서 "동일 조건 벤치마크"로 재정의.

**크리티컬 패스 파이프라인 시도, 실측 전에 계산으로 기각**: `ray_march_edt`
의 배럴 시프터 병목을 줄이려고 "룩업(LOOKUP)/적용(APPLY)" 2단 파이프라인
버전(`ray_march_edt_pipe.v`)을 만들어 기능검증까지 했으나(6/6 PASS, dist/hit
값 100% 일치), **사용자가 이 접근 자체의 근본 문제를 지적함**: 레이마칭은
"다음 스텝의 x/y가 이전 스텝 결과에 의존"하는 loop-carried dependency라서
일반적인 II=1(매 클럭 새 작업 시작) 파이프라인이 안 됨 — 스텝 하나가 그냥
1클럭에서 2클럭으로 늘어날 뿐임. 실측(6케이스 클럭 수 비교)으로 확인: 스텝당
클럭이 1.25~1.84배 증가(예: center_+x 19→35클럭). 크리티컬 패스 축소분
(예상 20~30%)이 이 클럭 수 증가를 상쇄 못 할 가능성이 높아 **합성해보기도
전에 net loss로 판단, 실험 폐기**(파일도 커밋 없이 삭제).

**대안 우선순위 확정 (사용자 제안)**:
1. RTL 안 건드리고 post-route 실측부터 — synth 추정치의 신뢰도 확인 (이번에
   완료)
2. 방향별 step-offset ROM — 60개 고정 라이다 빔이라는 이 프로젝트 특성을
   활용, 범용 가변 배럴시프터를 (beam_id, edt_shift)→(delta_x,delta_y)
   룩업으로 교체 시도(A/B 필요, 미착수)
3. 그 외 아이디어(shift 범위 축소, 다중 ray 컨텍스트 인터리빙, 전체
   사전계산)는 각각 정밀도 저하/설계 복잡도 급증/BRAM 재폭발 우려로 후순위

**post-route 실측 (`particle_scorer`, 파티클 1개, `synth_v3_route.tcl`)**:
opt_design → place_design → route_design까지 전부 실행 — 이 프로젝트 최초의
"진짜 배치·배선 이후" 타이밍.
| 지표 | post-synth 추정 | post-route 실측 |
|---|---|---|
| WNS | -2.594ns | **-2.370ns(오히려 더 좋음)** |
| 추정 fMax | ≈79.4MHz | **≈80.8MHz** |
| BRAM | 39/50(78%) | 39/50(78%, 동일) |
| DSP | 0 | 0(동일) |
| LUT | 1332(6.40%) | 1423(6.84%, 라우팅용 소폭 증가) |
| 크리티컬 패스 | `ray_march_edt` 배럴시프터(`y_reg`->`x_reg`), 12단 | **동일 경로, 13단**(라우팅 지연이 전체의 73.5% 차지 — post-route에서 흔한 패턴) |

**결론**: 지금까지의 post-synth 전용 수치(WNS, BRAM, DSP)가 실제 place&route
결과와 상당히 가깝다는 게 처음으로 확인됨 — 이 프로젝트의 "합성 추정치"가
과도하게 낙관적이지 않았다는 신뢰도 증거. 크리티컬 패스 정체도 재확인(같은
배럴 시프터). 아직 2/4/8파티클 구성은 post-route 미실시(1개 결과로 추세만
확인, 필요시 확장).

**이번에 건드린 파일**: `docs/problem_statement.md`(갭 분석 추가),
`synth/synth_v3_route.tcl` + `synth/util_v3_postroute.rpt` +
`synth/timing_v3_postroute*.rpt`. `ray_march_edt_pipe.v`/`tb_ray_march_edt_pipe.v`
는 만들었다가 폐기(커밋 안 함).

**남은 것**:
- 방향별 step-offset ROM A/B 실험(다음 세션 최우선 후보)
- 2/4/8파티클 구성도 post-route까지 확인할지 판단
- 동일 조건 벤치마크(60빔·500파티클 확장, ZERO와 직접 비교) — 큰 작업,
  별도 세션에서 스코프 잡고 착수 예정

## 2026-09-02 (이어서9) — 방향별 beam ROM A/B: 자원은 줄지만 타이밍은 근소 악화, 채택 안 함

**가설**: `ray_march_edt`의 크리티컬 패스(`y_reg`→`x_reg`, 배럴 시프터
`dx <<< cur_shift`)를, 이번 벤치마크가 고정 8빔(-60~60도 8등분)만 쓴다는 점을
이용해 `{beam_id, cur_shift}` → `(step_dx, step_dy)` 64칸 ROM 조회로 대체하면
빨라질 것이라는 가설. 스코프 한정을 코드 주석에 명시(`tools/gen_beam_step_rom.py`,
`rtl/ray_march_edt_beamrom.v` 상단): 실제 ZERO는 파티클 theta(연속값)에 따라
빔의 world-frame 방향이 매번 달라지므로, beam_id만 인덱싱하는 이 ROM은 이번
고정-8빔 벤치마크 안에서만 유효하고 그대로 실전에 못 씀(theta까지 양자화하면
ROM이 64 → 16000+로 커져 역효과 우려).

**구현**: `tools/gen_beam_step_rom.py`(ROM 생성, 기존 quantize 규칙과 동일 +
Verilog `<<<`의 18비트 truncate를 정수 시프트+마스킹으로 정확히 재현) →
`rtl/ray_march_edt_beamrom.v` + `rtl/particle_scorer_beamrom.v`(dx,dy 입력을
beam_id로 교체, 나머지 FSM은 particle_scorer.v와 완전 동일) →
`tb/tb_particle_scorer_beamrom.v`(기존 particle0 정답지 그대로 재사용 — beam_id
순서가 이미 -60~60도 8등분과 일치해서 새 오라클 불필요).

**기능 검증**: 시뮬레이션에서 weight_o=-7441로 기존 particle_scorer와 완전히
동일(cycle-by-cycle 타임스탬프까지 100% 일치, t=1315000ns) — ROM이 barrel
shift와 비트단위로 동등함을 확인.

**A/B 결과 (post-synth → post-route)**:
| 지표 | 기존(barrel) post-synth | beam ROM post-synth | 기존 post-route | beam ROM post-route |
|---|---|---|---|---|
| WNS | -2.594ns | -2.418ns (+6.8%) | **-2.370ns** | **-2.444ns (-3.1%, 더 나쁨)** |
| LUT | 1332 | 1228 (-7.8%) | 1423 | 1315 (-7.6%) |
| Registers | 253 | 218 (-13.8%) | 255 | 220 (-13.7%) |
| BRAM | 39/50 | 39/50(동일) | 39/50 | 39/50(동일) |
| DSP | 0 | 0 | 0 | 0 |
| Logic Levels(worst path) | 12 | 14 | 13 | 16 |

**결론 — post-synth 추정과 post-route 실측이 정반대 방향으로 뒤집힌 첫 사례.**
post-synth만 보면 "ROM이 이겼다"(WNS 6.8% 개선)로 오판할 뻔했으나, 실제
place&route 이후엔 오히려 baseline보다 3.1% 나쁨(critical path가 `x_reg`→`y_reg`
로 바뀌면서 logic level이 13→16으로 늘어남 — 작아진 설계를 배치기가 다르게
배치하며 라우팅 지연이 늘어난 것으로 추정, 배선 지연이 여전히 전체의 71~75%
차지). LUT/레지스터는 7~14% 일관되게 줄었지만, 이번 실험의 목표(클럭 속도
개선)는 달성 못함 — "post-synth 추정치가 실측과 상당히 가깝다"는 지난 검증
(2026-09-02 이어서8)과 달리, 이번엔 그 추정을 믿으면 안 되는 반례로 프로젝트
교훈에 추가.

**결정 (사전에 정한 우선순위 규칙 그대로 적용)**: 크리티컬 패스 개선 실패 →
`ray_march_edt.v`(기존 배럴시프터)를 계속 메인으로 유지. `ray_march_edt_beamrom.v`
/`particle_scorer_beamrom.v`는 "LUT/FF는 아끼고 싶지만 클럭은 상관없는" 상황을
위한 대안으로 rtl/에 남겨두되(동작 검증된 코드라 폐기하지 않음), README의
현재 권장 구성에는 반영 안 함.

**이번에 건드린 파일(커밋 대상)**: `tools/gen_beam_step_rom.py`(신규),
`rtl/ray_march_edt_beamrom.v`(신규), `rtl/particle_scorer_beamrom.v`(신규),
`tb/tb_particle_scorer_beamrom.v`(신규), `sim/beam_step_dx.hex`/`beam_step_dy.hex`
(신규, `gen_beam_step_rom.py`로 재생성 가능), `synth/synth_beamrom.tcl` +
`synth/synth_beamrom_route.tcl` + 해당 util/timing 리포트 4종(신규).

**남은 것**:
- 2/4/8파티클 구성 post-route 미실시
- 동일 조건 벤치마크(60빔·500파티클 확장, ZERO와 직접 비교) — 다음 세션
  최우선 후보, 이번 ROM 실험보다 스코프가 훨씬 큼

## 2026-09-02 (이어서10) — 동일 조건 벤치마크 첫 실측: ECHO가 이길 수도 있지만 조건이 많이 낙관적

**ZERO 쪽(range_libc) 실측**: `particle_filter.py`에 이미 있던 `fine_timing`
진단 훅을 로컬에서 켜고(임시, 측정 직후 되돌림 — 아래 사고 기록 참고)
표준 스모크 테스트(changwon, `sim:=true planner:=none controller:=pure_pursuit`)
로 145 샘플 실측. `range_method="rm"`(ECHO와 같은 레이마칭 계열),
`num_rays=60`, `max_particles=500` — 둘 다 ECHO 목표치와 정확히 일치:
ray casting 평균 1.294ms, sensor eval 평균 0.082ms, `sensor_model()` 전체
평균 1.428ms(WSL 노트북 CPU, **Jetson 실측 아님**).

**🔴 사고: `~/zero/src`에서 되돌리다가 관련 없는 로컬 변경사항을 실수로 날림.**
`fine_timing` 하나만 고쳤다가 `git checkout -- config/localize.yaml ...`으로
되돌렸는데, 그 파일엔 **이미 커밋 안 된 `max_particles: 500`(원래 4000,
2026-08-07에 opponent_perception CPU 병목 테스트용으로 임시 변경된 값) 오버라이드가
있었음** — 되돌리기 전에 `git status`/`git diff`로 "지금 내가 되돌리려는 게
정확히 뭔지" 확인을 안 하고 통째로 checkout해서 그것까지 같이 날아감. 다행히
**원격 저장소는 전혀 안 건드림(commit/push 없음, 로컬 워킹트리만 영향)** —
사용자에게 즉시 보고, 500/4000 중 어느 쪽으로 둘지 확인 대기 중(현재 4000,
커밋된 원래 값). 교훈: 다음부턴 이런 임시 측정용 수정은 `git checkout --`
대신 Edit으로 정확히 그 줄만 되돌릴 것 — 특히 남의(혹은 예전 세션의) 로컬
미커밋 변경이 이미 있을 수 있는 저장소에서는 checkout 전에 diff부터 볼 것.

**ECHO 쪽(60빔 확장) 실측**: RTL이 빔 개수에 파라미터화 안 돼있어 "빔
시작을 몇 번 주느냐"로만 정해짐 — 재합성 없이 테스트벤치만 60빔으로 확장
(`tools/gen_particle_scorer_60beam_test.py`, `tb_particle_scorer_60beam.v`,
`tb_particle_scorer_oct_arb_60beam.v`, 기존 SEARCH_CENTERS 8개 그대로 재사용).
전부 PASS(기존 8빔 오라클과 같은 시작점이라 particle0/p60_0 weight 교차검증도
됨). 실측 사이클(oct_arb post-synth WNS -2.438ns → 달성 가능 클럭 ≈80.4MHz
로 환산):
- 파티클 1개, 60빔: 959 사이클 = 11.93μs
- 파티클 8개 동시(arbiter8), 60빔: 1300 사이클 = 16.17μs (1개 대비 1.36배
  — 8배 병렬인데 36%만 손해, 여전히 효율적인 시분할)

**500파티클 추정(⚠️ 측정 아니라 계산)**: 8개씩 63배치를 순차로 돌린다고
가정 → 63 × 16.17μs ≈ **1.02ms**. 배치 간 파이프라이닝(레이마칭은 겹칠 수
있음)은 미설계라 이 값은 보수적 추정.

**비교**: ECHO 추정 1.02ms vs ZERO 실측 1.428ms → **이 조건에서 ECHO가 약
1.4배 빠를 수 있음** — 이 프로젝트에서 "이길 수도 있다"는 첫 정량적 신호.
**단, 8-way 병렬화가 없으면(파티클을 하나씩 순차 처리) 959사이클×500 ≈
5.97ms로 ZERO보다 4배 이상 느림** — arbiter 기반 병렬화가 이득이 아니라
"경쟁력을 갖추기 위한 필수조건"이라는 걸 숫자로 확인한 게 이번 세션의
가장 중요한 발견.

**남은 격차(정직하게, 우선순위순)**:
1. 위 1.428ms는 Jetson이 아니라 이 노트북 CPU 실측 — Jetson은 더 느릴 걸로
   예상되지만(그러면 ECHO가 더 유리해짐) 측정 안 함.
2. ECHO의 500파티클 값은 8파티클 배치 실측을 63배 곱한 추정이지, 실제
   500파티클 풀 스트리밍 시뮬레이션이 아님.
3. 삼각함수(cos/sin) 계산 비용 여전히 미측정 — `particle_scorer`가 빔마다
   `dx,dy`를 직접 받아서, Jetson이 파티클×60빔마다 이걸 미리 계산해줘야
   하는 비용이 위 range_libc 실측엔 안 잡혀있음(range_libc는 pose+각도만
   받아 내부에서 좌표변환).
4. Jetson↔FPGA 통신 비용 여전히 완전 미정.
5. 정확도 비교(weight 순위/ESS/localization 오차) 여전히 안 함.

**결론(정직하게)**: 순수 계산 사이클만 보면 ECHO가 이길 가능성을 처음으로
숫자로 보여줬지만, "Jetson이 아닌 노트북과, 통신비용 0원인 FPGA"를 비교한
낙관적 상한에 가까움 — 위 3·4번이 실측에 들어가면 이득이 줄어들 것이
거의 확실. `docs/problem_statement.md`의 "2026-09-02 추가2" 섹션에 전체
내용 반영.

**이번에 건드린 파일(ECHO, 커밋 대상)**: `tools/gen_particle_scorer_60beam_test.py`
(신규), `tb/tb_particle_scorer_60beam.v` + `tb/tb_particle_scorer_oct_arb_60beam.v`
(신규), `sim/p60_{0..7}_{dx,dy,r,expected,x0y0}.hex`(신규, 40개),
`docs/problem_statement.md`(갱신).
**ZERO 쪽(`~/zero/src`)은 전부 로컬 전용, 위 사고 기록대로 최종적으로는
원복됨(fine_timing=0, max_particles는 사용자 확인 대기 중) — git 변경 없음.**

**다음 세션 우선순위**:
1. `max_particles` 500/4000 여부 사용자와 확정
2. 500(또는 4000) 파티클 풀 스트리밍 ECHO 테스트벤치(추정 아닌 실측)
3. 삼각함수 계산 비용 측정(Jetson 쪽 전처리 비용)
4. 정확도 비교(weight 순위/ESS) — 이번 세션 5단계 계획의 4번
5. Jetson↔FPGA 통신 비용 설계·측정 — 5단계 계획의 5번

