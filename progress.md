# ECHO 진행 기록

## 2026-09-01 — 프로젝트 시작, 문제정의, 센서모델 PE 1개 검증 완료

**시작**: ZERO에서 겪은 실제 병목을 이번 학기 회로설계 수업 프로젝트로 가속해보자는
아이디어로 착수.

**시장조사**: 같은 F1TENTH particle_filter를 FPGA로 가속한 선행논문 실제로 확인함
— Bernardi et al., *"An FPGA Overlay for Efficient Real-Time Localization in
1/10th Scale Autonomous Vehicles"* (DATE 2022). 놀랍게도 ZERO가 쓰는 것과 **완전히
같은 원본 소프트웨어**(Walsh & Karaman, ICRA 2018 reference implementation)를
베이스로 함. 이 논문은:
- Xilinx Zynq UltraScale+ (Ultra96/ZCU102, 산업용 대형 보드)
- Vivado **HLS**로 C++→RTL 자동합성 (손으로 짠 RTL 아님)
- 레이마칭만 하드웨어 가속(전체 지연의 90%), 센서모델 평가는 소프트코어에서
  그대로 실행(데이터 이동만 줄임, 병렬 하드웨어화 안 함)

→ **차별화 지점 확정**: 손으로 짠 RTL, 소형 교육용 보드, 그리고 논문이 안 건드린
센서모델 평가를 v1 타깃으로.

**실측 (가장 중요한 작업)**: `particle_filter.py`의 `fine_timing` 파라미터를 켜고
ZERO 시뮬레이션(`sim:=true planner:=none`, opponent_perception 없이 PF 단독)을
실제로 돌려서 진짜 숫자를 뽑음.
- 레이마칭이 센서모델 계산의 **89.6%(500파티클) / 90~95%(4000파티클)** — 논문
  수치(90%)와 3중 일치
- **500파티클: possible ≈ 620Hz(요구 40Hz 대비 15배 여유) / 4000파티클(설계 목표):
  possible ≈ 88Hz(2.2배 여유)** — 이 개발PC에서는 PF가 병목이었던 적이 한 번도 없음
- 결론: `max_particles`가 4000→500으로 다운그레이드된 채 방치된(2026-08-07~) 진짜
  원인은 PF 자체가 아니라 그때 같이 돌던 `opponent_perception`(O(n²) 버그)일 가능성이
  높음. Jetson 실측은 여전히 미실측(가장 큰 남은 갭) — 상세는
  [`docs/problem_statement.md`](docs/problem_statement.md).

**진단 중 발견한 버그(재발 방지용 기록)**: `ros_clean.sh`와 `ros2 launch ...`를 같은
`bash -lc "..."` 문자열 안에서 연달아 실행하면, `ros_clean.sh`의
`pkill -f 'ros2 launc[h]'`가 **그 문자열 자체(나중에 나올 "ros2 launch" 텍스트를
이미 포함)를 실행 중인 셸 프로세스 자신**에 매칭돼서 자기 자신을 죽여버림(exit 15).
반드시 별개의 명령으로 분리해서 실행할 것. 또한 `bash -lc`(비대화형)는 `.bashrc`의
인터랙티브 체크 때문에 ROS 환경이 자동 로드 안 됨 — 매번 4줄(foxy/venv/install/
PYTHONPATH) 명시적으로 source 필요.

**타깃 확정**: 레이마칭이 임팩트는 훨씬 크지만(3중 확인), **v1은 센서모델 평가로
확정, 레이마칭은 v2.** 이미 설계가 끝나있고 난이도가 첫 프로젝트에 적당함.

**설계 & 구현**:
- `echo-ref/gen_sensor_model.py`(Windows) — ZERO의 `precompute_sensor_model()`
  수식을 그대로 이식. 표를 log(확률)로 저장해서 RTL이 곱셈기 없이 룩업+덧셈만
  하면 되게 설계(squash_factor도 log영역에선 상수곱). 실측 log(prob) 범위
  [-9.10, -2.12]에서 역산한 **Q5.8(13비트)** 고정소수점, 테이블 301×301(15m/5cm),
  전체 143.8KB(추후 보드 BRAM 예산과 대조 필요).
- `rtl/sensor_pe.v` — PE 1개. BRAM 1클럭 읽기지연에 맞춘 파이프라인(주소 제시 →
  다음클럭 데이터 도착 → 누적). `start`/`valid_i`/`last_i`/`done` 인터페이스.
- `rtl/table_mem.v` — `.hex` 로드하는 동기식 BRAM.
- `tb/tb_sensor_pe.v` — 파이썬이 만든 파티클 5개(8빔 장난감 시나리오)로 검증.

**검증 — 첫 실행 2/5 실패, 원인 규명 후 5/5 통과**: 처음 돌렸을 때 5개 중 2개가
±1 LSB(=1/256) 차이로 실패. 원인은 RTL 버그가 아니라 **정답지 생성 방식의
반올림 순서 실수**였음 — 파이썬이 "float 8개를 다 더한 뒤 한 번에 반올림"했는데,
RTL(진짜 하드웨어)은 "이미 반올림된 표 값 8개를 정수로 더함" — 이 둘은 수학적으로
다른 결과가 나올 수 있음. 실제로 문제였던 두 파티클의 표 항목을 직접 더해보니
RTL 결과(-12229)가 맞고 파이썬 정답지(-12230)가 틀렸음을 확인. `gen_sensor_model.py`
를 "양자화된 표 값을 정수로 더하는" 방식으로 수정 → 재생성 → **5/5 완전 일치.**

**60빔 확장 + PE 2개 병렬화 (같은 세션 이어서 진행)**:
- `gen_sensor_model.py`의 테스트벡터 출력 방식을 바꿈 — 예전엔 사람이 읽는
  텍스트(`obs=[...] addrs=[...]`, 파이썬 리스트 형식)를 Verilog 소스에 손으로
  옮겨적었는데, 빔이 8→60개로 늘면서(파티클 5개 x 60빔 = 300개) 손으로 옮겨적는
  게 비현실적이 됨. `testvec_addrs.hex`/`testvec_expected.hex` 두 개로 바꿔서
  테스트벤치가 `$readmemh`로 직접 읽게 함 — 데이터를 소스에 손으로 옮기지 않는
  게 원칙(오타를 RTL 버그와 헷갈릴 여지를 원천 차단).
- `tb/tb_sensor_pe.v`를 60빔 기준으로 다시 검증 — **5/5 PASS**, RTL은 한 글자도
  안 고침(PE가 빔 개수를 하드코딩 안 해서).
- **병렬화**: `rtl/table_mem_dp.v`(듀얼포트 BRAM, 포트 2개가 같은 `mem` 배열을
  각자 독립적으로 읽음 — 테이블을 2벌 복사하는 게 아니라 진짜 듀얼포트 메모리
  1개로 인식되는 표준 패턴) + `tb/tb_sensor_pe_parallel.v`(PE 2개를 `fork/join`
  으로 정확히 같은 타이밍에 구동, 각각 파티클 0/1을 동시 처리).
- 결과: **두 PE 다 정답과 일치**, 그리고 **총 소요시간이 640ns**(≈ 파티클 1개
  처리 시간, 62클럭×10ns)로 — 순서대로 처리했으면 거의 2배 걸렸을 텐데 그렇지
  않았음 = **진짜 동시 처리가 검증됨.**

**PE 4개로 확장**: BRAM 포트가 2개뿐이라(듀얼포트), PE 4개를 진짜로 동시에
돌리려면 **테이블 복사본 2개**가 필요함(PE0,1은 복사본0 / PE2,3은 복사본1) —
BRAM 143.8KB → 287.6KB(2배), 나중에 보드 정해지면 이 비용이 부담되는지 판단 필요.
`sensor_pe.v`/`table_mem_dp.v`는 손 안 대고 `generate`로 4개/2개 인스턴스화만 함.

**🔴 무한루프 버그, 원인 규명**: 처음 돌렸을 때 `vvp`가 CPU 100%로 6분 넘게
안 끝남(진짜 무한루프, 멈춘 게 아니라 계속 도는 중이었음 — `clk`가 영원히
토글되는데 `$finish`에 도달을 못 함). 원인: `task drive_pe(pe, idx)` 하나를
`fork`로 4번 동시에 호출했는데, Verilog `task`는 기본이 **정적(static)**이라
동시 호출끼리 내부변수(`rr`, 심지어 입력 `pe`/`idx`까지)를 **공유**함 — 4개가
서로 덮어쓰면서 최소 1개 PE가 `last_i`를 제대로 못 받아 `done`이 영원히 안 뜸.
(PE 2개 버전은 `drive_pe0`/`drive_pe1`을 아예 다른 task로 나눠서 우연히 이
문제를 피했었음.) **해결**: `task automatic drive_pe(...)`로 `automatic` 키워드
추가 — 호출마다 독립된 저장공간을 쓰게 함. 재컴파일 후 **4/4 PASS, 총 소요시간
640ns**(PE 2개 버전과 완전히 동일) — 파티클이 2개든 4개든 처리시간이 그대로라는
게 "진짜 병렬"이라는 걸 다시 한번 확인함.

**"진짜 4배냐" 직접 대조 실측**: 지금까지는 "PE 2개일 때도 640ns, 4개일 때도
640ns"라는 간접 증거만 있었음. `tb/tb_sensor_pe_seq4.v`(PE 1개로 파티클 4개를
순서대로 처리하는 대조군)를 새로 만들어서 직접 비교:
- PE 1개, 파티클 4개 순서대로: **2560ns**
- PE 4개, 파티클 4개 동시에: **640ns**
- 2560 ÷ 640 = **정확히 4.0배** — 추측이 아니라 같은 조건 직접 대조로 확정.

**남은 것**:
1. ~~8빔 장난감 → 실제 스펙 60빔~~ — ✅ 완료
2. ~~PE 2개 병렬화~~ — ✅ 완료(듀얼포트 메모리 공유 방식으로 검증)
3. ~~PE 4개로 확장~~ — ✅ 완료(테이블 복사본 2개, task automatic 버그 수정 포함)
4. PE를 더 늘리기(8개, 16개...) — 테이블 복사본을 계속 늘리는 게 맞는지, 아니면
   어느 지점부터 시분할(중재) 로직으로 바꿔야 하는지 — 보드 BRAM 예산 알아야 결정 가능
5. 주소생성 로직 — 지금은 테스트벤치가 `r*301+d`를 미리 계산해서 줌. 301이
   2의 거듭제곱이 아니라 곱셈 없이 하드웨어로 만드는 게 아직 v1 스코프 밖
6. Jetson에서 `fine_timing` 실측 — problem_statement.md의 가장 중요한 미해결 갭
7. FPGA 보드 확정되면 합성(Vivado/Quartus) — 비트폭·PE개수·BRAM예산 최종 확정
8. v2: 레이마칭 PE (더 어려움 — 가변 길이 격자 순회, 훨씬 큰 온칩 메모리 필요)
## 2026-09-02 — 주소생성 로직(곱셈기 없이 r*301+d)

**설계**: 301은 2의 거듭제곱이 아니라 시프트만으로 곱셈을 대신할 수 없음. 하지만
"상수" 곱셈은 그 상수를 2의 거듭제곱의 합으로 쪼개서 시프트+덧셈으로 풀 수 있음:
```
301 = 256+32+8+4+1 = 2^8+2^5+2^3+2^2+2^0
r*301 = (r<<8)+(r<<5)+(r<<3)+(r<<2)+r
```
진짜 곱셈기(부분곱 N개를 다 더해야 함) 대신 **덧셈 4번**으로 끝남 — `rtl/addr_gen.v`.
테이블을 512로 패딩해서 시프트만으로 푸는 대안도 있었지만(그럼 곱셈이 완전히
사라짐), 테이블이 143.8KB→426.7KB(3배)로 커져서 기각 — BRAM을 아끼는 게 더
중요하다고 판단(PE 병렬화 때 BRAM 비용 문제를 이미 겪어봐서).

**검증 2단계**:
1. `tb/tb_addr_gen.v` — addr_gen 단독, 손으로 계산한 (r,d)→주소 6쌍과 대조. 6/6 PASS.
   (r=300,d=300 → 90600으로 테이블 맨 끝 주소까지 확인함)
2. `tb/tb_sensor_pe_addrgen.v` — addr_gen을 sensor_pe 앞단에 실제로 연결해서,
   테스트벤치가 이제 **주소가 아니라 원본 (r,d) 값만** 주고, 하드웨어가 스스로
   주소를 계산해 룩업+누적까지 끝내는 전체 파이프라인 검증. 5/5 PASS, 예전
   버전(주소 미리 계산해서 줬을 때)과 **정확히 같은 weight 값** — addr_gen이
   기존에 쓰던 정답 주소와 완전히 같은 걸 만들어낸다는 교차검증까지 확보.

**의미**: 이제 RTL 파이프라인에 "테스트벤치가 몰래 대신 계산해준 값"이 하나도
없음 — 룩업 주소부터 누적, 병렬 처리까지 전부 하드웨어 설계로 완성됨. v1(센서모델
가속기) 핵심 요소는 다 갖춰졌다고 볼 수 있음.

**gen_sensor_model.py 변경**: 테스트벡터에 `testvec_r.hex`/`testvec_d.hex`(원본
r,d 쌍) 추가 — 기존 `testvec_addrs.hex`(미리 계산된 주소)는 addr_gen 없이 PE만
테스트할 때 계속 씀.

**다음(3번, 레이마칭 v2)으로 넘어가기 전 남은 v1 항목**:
- PE를 4개보다 늘리기 — 보드 BRAM 예산 알아야 결정 가능(계속 보류)
- Jetson 실측 — 실차 접근 기회 필요(계속 보류)
- 합성 — 보드 확정 필요(계속 보류)
